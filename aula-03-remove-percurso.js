(() => {
  const p = document.querySelector('#percurso .section-heading p:last-child');
  if (p?.textContent.includes('Siga a sequência. As atividades foram organizadas para que a representação apareça como resposta a uma necessidade, e não como uma lista de definições.')) p.remove();
})();
